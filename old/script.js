var animation = bodymovin.loadAnimation({
    container: document.getElementById('anim'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'webdesignanim.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('head'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'header.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('mobile-head'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'mobile-head.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('webdesignscript'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'webdesignscript.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('webscript'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'webscript.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('productscript'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'productscript.json'
})

var animation = bodymovin.loadAnimation({
    container: document.getElementById('portraitscript'),
    renderer: 'svg',
    loop: false,
    autoplay: true,
    path: 'portraitscript.json'
})